# Blackstar — engineering completion ledger

Last reconciled: 2026-09-22. Baseline: production `dd041e146debb8eefc603663d7246447d7d9b093` READY. This ledger tracks the **requested full Blackstar vision**, not just whether a screen or code branch exists. It is deliberately not a numerical estimate of overall product completion.

## How to mark an item complete

An item starts OPEN. Record the actual files/PR/migrations, CI on the exact feature head, merge SHA, production deployment ID and matching SHA, and—where applicable—signed-in real-provider acceptance evidence. A READY deployment alone is not proof of a business workflow, connected API, model inference, external side effect or independent certification. Use BLOCKED_EXTERNAL only when a real provider account, qualified authority, paid plan, mobile hardware or operator action is required. Never substitute a fake result or silently mark a provider-gated capability complete.

The separate [operator verification queue](blackstar-user-verification-queue.md) contains ONLY checks requiring the owner's action. Anything engineering can inspect, implement, test, deploy or observe should stay here, not be handed to the owner. Never ask for passwords, payment-card details or API keys in a chat; use the provider's own consent flow or the server-side secret manager.

**Progress accounting:** The code/deployment slice may be complete while end-to-end acceptance is pending. Do not call that feature 100% until the acceptance evidence exists. Percentages are optional only when scoped, measured against a written denominator, and distinguish unverified work from completed work. Do not treat the overall platform as a simple average of unrelated modules.

## Engineering backlog

| ID | Programme | Engineering-side remaining work / evidence to collect | Operator dependency | Status |
| --- | --- | --- | --- | --- |
| E01 | Agent memory and knowledge | Existing owner-scoped memory fabric, lexical/vector document retrieval, provenance, retention and operator preview are deployed. Run auth/agent-scope/expiry/relevance/negative-path regression and real session retrieval against owner-authorised records; capture observed results and review any faults. No synthetic records in an owner's production account. | U01 for real owner data and sign-in | OPEN — live acceptance |
| E02 | Runtime and crash recovery | Reconcile stale PR #646. Surface real history/config/cancellation DB failures and retire obsolete manual reaper without modifying canonical scheduled worker. Verify exact head, merge, deploy and recovery evidence. | None for coding and CI; U02 only if user wants their own agent run tested | IN_PROGRESS |
| E03 | Bounded General Intelligence / Astra | Inventory implemented model serving and exact pinned candidate, readiness, evaluator and approvals. Run real independent cases with provenance, score against defined acceptance criteria; no AGI or GPT parity claims. | U03 only if local/native model or separate hardware/provider activation is needed | OPEN — certification |
| E04 | 160 agent playbooks | Verify installation, security scanning, matching to granted skills, disabled-skill behaviour and representative governed agent runs. Do not equate guidance text with new executable tools. | U04 to install the pack in their workspace if not installed | OPEN — usage certification |
| E05 | 160 AI Workbench workflows | Validate signed-in authenticated model responses, errors/quotas and representative distinct outputs per category; verify all catalogue entries are callable. Do not represent 160 prompts as 160 autonomous agents. | U05 for own account acceptance when needed | OPEN — live acceptance |
| E06 | Core integrations, actions, MCP and approvals | Audit actual deployed action manifests, provider scope checks, idempotency and approval outcomes; repair missing connectors using canonical runtime without Nango lock-in. | U06 where official provider consent/credentials are required | OPEN — provider-by-provider |
| E07 | Cinema Studio | Inspect real Seedream/LTX/render-worker configuration and shot pipeline, retry/assembly errors, full output evidence and long-film feasibility. Gate unavailable providers honestly. | U07 only for paid/contracted worker or provider activation | OPEN — operational rendering |
| E08 | Game Foundry | Verify prompt-to-3D worker, engine export and actual playable build or asset handoff with real workers; fix observed failures, preserve project history. | U08 for licensed engine/worker accounts where required | OPEN — runtime certification |
| E09 | Trading Hub | Verify persisted watchlists/paper trading, journal, provider-as-of market series, alerts and Quant/Finance handoff. Keep real-money broker/trading and uncovered feeds provider-gated. | U09 only for chosen official live feeds or brokerage permissions | OPEN — live provider coverage |
| E10 | Marketplace and financial lifecycle | Verify precise fees, seller onboarding, Stripe Connect/live subscription/webhook lifecycle, refund/dispute paths, audit and consent; do not settle real charges without owner approval. | U10 for Stripe business onboarding and any consent to real-money test | OPEN — live settlement certification |
| E11 | Website Studio | Compare stale PR #499 to current builder; cherry-pick only missing footer/component work. Test full multi-page generation, edit, revision, publishing and rollback. | U11 only for external custom domain/provider setup | OPEN — gap audit and end-to-end |
| E12 | Construction & Industrial | Reconcile unmerged PRs #582/#583/#588/#589/#592 against current schema; implement only missing planning, digital asset, inspection, bounded agents and portfolio surfaces. Verify engineering-safety approval gates. | U12 only if qualified person sign-off or real industrial hardware is needed | OPEN — stale PR reconciliation |
| E13 | Retail & Local Business | Reconcile PRs #533/#546/#547/#566, connect only actually available approved booking reminder channels; verify delivery states, inventory and booking transactions. | U13 for store/account/provider consent or legitimate customer message test | OPEN — connected operations |
| E14 | Inbound Retail phone receptionist | Verify carrier webhook signature, number-pairing and 20-turn bounds, approved action queue and phone-call evidence. | U14 for Twilio number, carrier permissions and actual test call | BLOCKED_EXTERNAL for real call |
| E15 | Dropshipping Hub | Audit unmerged PRs #522/#531/#542/#544/#545/#549/#552, reconcile against live catalogue, supplier and listing workflows. Verify immutable approval/publish evidence on supported connected providers. | U15 for real merchant/supplier account consent | OPEN — provider certification |
| E16 | Food delivery adapters | Enforce official per-market capability manifest and explicit location/approval rules; implement/verify only authorised consumer, merchant and courier APIs, no provider parity fiction. | U16 for provider partner access where an action requires it | BLOCKED_EXTERNAL for restricted APIs |
| E17 | Mobile AI/device bridge | Test existing native iOS/Android adapters, paired identity, on-device capability reporting and fallbacks. Do not claim unsupported Apple/Gemini feature integration. | U17 for native-device builds and hardware/OS checks | OPEN — native-device verification |
| E18 | Company Hub | Reconcile PR #489 against main; verify source-backed KPI, owned workspaces, Finance/CRM/agent handoffs, permissions and complete production contract. | U18 only for owner-entered business source records | OPEN — reconciliation |
| E19 | Industry, Finance, Legal and Compliance Sentinel | Audit coverage versus verifiable sources, regulatory scheduler freshness, change-review approval and alert delivery; avoid blanket worldwide legal/finance guarantees. | U19 for licensed/legal/compliance specialist review where legally required | OPEN — coverage and alert certification |
| E20 | Project repositories/collaboration | Verify project ownership, private/public/permission boundaries, revocation, versioning and safe sharing using non-owner test identities. | U20 if owner chooses to invite an actual collaborator | OPEN — access acceptance |
| E21 | Phone-call project/company context | Reconcile unmerged PR #600 with existing telephony, consent and existing Projects/Company Hub retrieval; avoid broad automatic disclosure. | U21 for a real permitted call only after provider ready | OPEN — reconciliation |
| E22 | Platform-wide 3D theme, desktop and mobile | Audit every route, responsive layout, keyboard/a11y, performance and dark-violet design. Repair only actual gaps rather than reskinning finished screens. | U22 only for personal device visual sign-off | OPEN — route matrix |
| E23 | Production provider/security readiness | Audit enabled providers, auth email-confirmation behaviour, Stripe live and Routes API capabilities, RLS/data grants, observability and genuine end-to-end approval. Earlier readiness docs are dated and cannot certify current config. | U23 for provider business verification and identity/account settings | OPEN — current-state audit |
| E24 | Universal AI Hub / enterprise ten pillars | Convert open master issue #185 into bounded per-capability contracts for identity, builder, marketplace, compute, data, certification and portability; track only demonstrated operational capability. | U24 only for paid enterprise/on-prem accounts or explicit strategic scope decisions | OPEN — scope certification |

## Immediate engineering sequence

1. Finish E02's minimal current-main recovery patch; run Backend Check and exact-head preview, merge, and verify matching production SHA. Do not merge stale PR #646 on top of newer main without reconciliation.
2. Run E01's signed-in read-only recall and privacy tests when appropriate owner-authorised data exists; otherwise mark **no data available** and keep it open without creating fictional user records.
3. Audit each stale PR against main before implementing or merging. A PR remaining open is a **review item**, not proof that its changes are missing.
4. Iterate through E03–E24 in bounded, individually tested and deployed batches. Transfer only provider/device/owner-specific checks to the operator queue; never hide engineering failures as user dependencies.

## Evidence log

- 2026-09-22: latest verified production merge `dd041e14` READY; initial ledger reconciled from current main, 20 open PRs, existing completion contracts and the active memory schema. This is **not** an end-to-end certification of every area.
- 2026-09-22: read-only active production database check found zero saved agent memories, zero memory documents/chunks, and zero installed agent skills. E01 and E04 remain owner-data/workspace-acceptance dependent; do not claim their live recall or skill selection is verified.
- E02 initial audit (baseline `dd041e14`): the user-triggered legacy `reap_stale_agent_tasks` RPC was still present and task read failures appeared as empty history. The linked recovery patch retires that endpoint and handles read errors explicitly. Confirm matching production SHA and real-run acceptance before closing E02.
