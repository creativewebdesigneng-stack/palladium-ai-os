import Builder from './Builder';

/**
 * Legacy AI Builder route.
 *
 * Keep both navigation entry points on the same truthful production-readiness
 * screen until repository, sandbox and deployment backends are connected.
 */
export default Builder;
