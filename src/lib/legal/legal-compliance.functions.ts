import{createServerFn}from"@tanstack/react-start";import{z}from"zod";import{requireSupabaseAuth}from"@/integrations/supabase/auth-middleware";type Sb={from:(t:string)=>any};
const schema=z.object({id:z.string().uuid().optional(),title:z.string().trim().min(1).max(180),jurisdiction:z.string().trim().min(1).max(120),authority:z.string().trim().max(180).optional(),source_url:z.string().url().optional().or(z.literal("")),category:z.string().trim().max(80).default("general"),owner_name:z.string().trim().max(120).optional(),status:z.enum(["review","applicable","implemented","not_applicable","monitor"]).default("review"),review_on:z.string().regex(/^\d{4}-\d{2}-\d{2}$/).optional().or(z.literal("")),notes:z.string().trim().max(4000).optional()});
export const listLegalComplianceObligations=createServerFn({method:"POST"}).middleware([requireSupabaseAuth]).handler(async({context})=>{const sb=context.supabase as unknown as Sb;const{data,error}=await sb.from("legal_compliance_obligations").select("*").order("review_on",{ascending:true,nullsFirst:false});if(error)throw new Error(error.message);return data??[];});
export const saveLegalComplianceObligation=createServerFn({method:"POST"}).middleware([requireSupabaseAuth]).inputValidator((v:unknown)=>schema.parse(v)).handler(async({data,context})=>{const sb=context.supabase as unknown as Sb;const row={title:data.title,jurisdiction:data.jurisdiction,authority:data.authority||null,source_url:data.source_url||null,category:data.category,owner_name:data.owner_name||null,status:data.status,review_on:data.review_on||null,notes:data.notes||null,updated_at:new Date().toISOString()};if(data.id){const{data:o,error}=await sb.from("legal_compliance_obligations").update(row).eq("id",data.id).select().single();if(error)throw new Error(error.message);return o;}const{data:o,error}=await sb.from("legal_compliance_obligations").insert({...row,user_id:context.userId}).select().single();if(error)throw new Error(error.message);return o;});
export const deleteLegalComplianceObligation=createServerFn({method:"POST"}).middleware([requireSupabaseAuth]).inputValidator((v:unknown)=>z.object({id:z.string().uuid()}).parse(v)).handler(async({data,context})=>{const sb=context.supabase as unknown as Sb;const{error}=await sb.from("legal_compliance_obligations").delete().eq("id",data.id);if(error)throw new Error(error.message);return{ok:true};});
