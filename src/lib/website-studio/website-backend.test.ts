import {describe,expect,it} from 'vitest';import {compileWebsiteBackendSql,safeSqlIdentifier} from './website-backend';
describe('website backend compiler',()=>{it('normalizes SQL identifiers',()=>{expect(safeSqlIdentifier('Customer Leads')).toBe('customer_leads')});it('generates owner-scoped RLS and grants',()=>{const out=compileWebsiteBackendSql({collections:[{name:'Leads',fields:[{name:'email',type:'text',required:true}]}]});expect(out.sql).toContain('create table if not exists public.leads');expect(out.sql).toContain('(select auth.uid()) = user_id');expect(out.sql).toContain('revoke all on public.leads from anon');expect(out.sql).toContain('email text not null')});it('warns when auth still needs provider provisioning',()=>{expect(compileWebsiteBackendSql({auth:{enabled:true,providers:['google']}}).warnings.length).toBeGreaterThan(0)});});