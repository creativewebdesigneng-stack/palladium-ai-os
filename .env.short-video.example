# PalladiumAI automated short-video worker
# Server-only deployment values. Do not expose these with VITE_ prefixes.
# Worker contract: POST /jobs, GET /jobs/:id.
# The worker owns stock/generated media assembly, narration, subtitle alignment,
# background-music mixing, transitions and final video rendering.
# PalladiumAI remains authoritative for auth, model-driven script planning, RLS,
# job history, audit state and downstream publishing through Social Operations.
SHORT_VIDEO_WORKER_URL="https://your-short-video-worker.example"
SHORT_VIDEO_WORKER_TOKEN="replace-in-your-secret-manager"
